# Handoff notes, 2026-09-07

## Where things stand

Code is done, tested, and pushed. `origin/main` is at `9b3ec80`. 106 tests pass.
The Oracle box has none of it yet because SSH won't connect.

## Blocked: SSH to the Oracle VM

`ssh -i ~/.ssh/oracle_cloud ubuntu@130.210.55.97` fails with
"Connection timed out during banner exchange", meaning TCP reaches port 22 but
sshd never answers.

Already ruled out:

- Instance `stock-agent` is RUNNING (ap-mumbai-1)
- Public IP is still 130.210.55.97, unchanged
- No network security groups on the vnic
- The subnet uses exactly the security list `ssh-connect.sh` edits
  (`ocid1.securitylist.oc1.ap-mumbai-1.aaaaaaaaaqift5luhxm6fx5tc7hj23cwtd3i2ubjd2bdeyt65pd37nyg7yrq`)
- That list's only ingress rule now allows `134.238.241.162/32` on port 22,
  which matches the current outbound IP from both ifconfig.me and ipify
- Three retries, same result

So the cloud firewall is fine. Next places to look: the VM's own iptables (OCI
Ubuntu images ship with rules in `/etc/iptables/rules.v4`), a hung or full
instance, or sshd not running. Use the OCI serial console or Cloud Shell to get
in, since both bypass the public network path.

## What to deploy once SSH works

    cd /opt/stock-agent
    git pull
    # edit .env, see below
    sudo -u stockagent bash -c 'export DOCKER_UID=997 DOCKER_GID=997 && docker compose up -d --build'
    sudo docker logs stock-agent-stock-agent-1 --tail 30
    sudo systemctl start stock-agent-watchdog.timer

### The server's `.env` needs four changes

The local `.env` is already correct; the server's is not. Set:

    LLM_PROVIDER=gemini
    GEMINI_API_KEY=<the key>
    TIER1_MODEL=gemini-3.5-flash-lite
    TIER2_MODEL=gemini-3.6-flash
    WATCHED_CHANNELS=-1004323736609,8718990088

Anything older than `gemini-3.x` 404s. `gemini-2.0-flash` and `gemini-2.5-flash`
are both retired.

### Watch out on first boot

The message watermark now lives in the DB. The server's existing `data/agent.db`
already has watermarks for the three chats being dropped, which is harmless. The
bot chat `8718990088` is new, so its first poll records the current message id
and processes nothing. That is the intended behaviour, not a failure.

## What changed in this session

- `get_balance` read `available_balance`, which the INDstocks `/funds` endpoint
  doesn't return, so the wallet always showed 0. Real value is at
  `data.detailed_avl_balance.eq_cnc`. Verified live at 10,000.
- Broker and LLM calls retry twice with exponential backoff. Orders never retry.
  429 from the broker raises immediately so the auth cooldown still works.
- 403 re-auth is capped at one per request via a `reauthed` flag.
- Switched to Gemini. `config.py` raises on an unknown `LLM_PROVIDER`.
- `main.py` calls `get_dialogs()` after the user client connects, otherwise bare
  numeric IDs for bot and user chats can't resolve.
- `telegram_reader` no longer replays history. First poll per chat records the
  newest message id and returns nothing; later polls read only past that mark.
- Watched chats cut from five to two: RangaOne Premium (`-1004323736609`) and
  the sanika_post bot (`8718990088`). The other three included Sanika's Premium
  Community, dead for 292 days.
- Telegram `api_id` and `api_hash` were scrubbed from all 27 commits with
  `git filter-repo` and force pushed. Backup mirror at
  `/Users/jagshep/IdeaProjects/sanikaStocks-backup.git`.

## Still owed

1. Email reports@telegram.org to reset the `api_hash` for `api_id 30261858`.
   It sat in a public repo. Draft is in the previous session.
2. After that, email support@github.com to purge unreachable objects on
   `PJagshettiwar/sanikaStocks`, so the old commits stop being fetchable by SHA.
3. `scripts/run_once.py` runs the whole pipeline against one chat and stops at
   approval instead of trading. Useful when the market is closed:
   `.venv/bin/python scripts/run_once.py sanika_post_bot`
